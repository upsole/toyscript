val fibonacci = fn(x) {
	if (x < 2) {
		return x;
	}
	return fibonacci(x - 1) + fibonacci(x - 2);
}

fn (x) {
	val recur = fn(i){
		print(fibonacci(i));
		if (i < x) {
			recur(i + 1);
		}
	};
	recur(0);
}(15);
